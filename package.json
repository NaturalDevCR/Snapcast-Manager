{
  "name": "snapcast-manager",
  "private": true,
  "version": "0.1.11",
  "scripts": {
    "version": "node scripts/sync-version.js",
    "postversion": "git add client/package.json server/package.json scripts/install.sh && git commit --amend --no-edit"
  }
}
